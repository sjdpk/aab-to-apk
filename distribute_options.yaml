output: dist/
releases:
  - name: release
    jobs:
      - name: release-deb
        package:
          platform: linux
          target: deb
          build_args:
            enable-experiment: records